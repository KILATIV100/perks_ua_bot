{
  "name": "perkup",
  "version": "1.0.0",
  "private": true,
  "description": "PerkUp - Coffee shop ecosystem with Telegram Mini App",
  "scripts": {
    "server": "npm run start --prefix server",
    "bot": "npm run start --prefix bot",
    "client": "npm run build --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix bot && npm install --prefix client",
    "db:generate": "npm run db:generate --prefix server",
    "db:push": "npm run db:push --prefix server",
    "db:seed": "npm run db:seed --prefix server"
  },
  "keywords": ["telegram", "mini-app", "coffee", "fastify", "prisma"],
  "license": "MIT"
}
